<?xml version="1.0" encoding="UTF-8"?>

<!-- user.xml -->
<!DOCTYPE mapper
   PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
   "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="Cart_Team1">
	<select id="selectCart" parameterType="String" resultType="shop.CartDTO">
		select * from Cart_Team1 where prodNum=#{prodNum}
	</select>




	<!-- delete x modify x insert o -->
</mapper>