<?xml version="1.0" encoding="UTF-8"?>

<!-- user.xml -->
<!DOCTYPE mapper
   PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
   "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="Order_Team1">
	<select id="selectOrder" parameterType="String" resultType="shop.OrdersDTO">
		select * from Order_Team1 where orderNum=#{orderNum}
	</select>
</mapper>