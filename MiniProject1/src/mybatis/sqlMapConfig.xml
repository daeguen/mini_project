<?xml version="1.0" encoding="UTF-8"?>
<!--  -->
<!DOCTYPE configuration
	PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-config.dtd">
  
<configuration>
	<!-- DB 정보 로드 -->
	<properties resource="mybatis/connect.properties"/>
	<!-- 이클립스는 기본적으로 src가 현재 경로 -->
	
	<!-- DB 연결 설정 -->
	<environments default="development">
		<environment id="development">
			<transactionManager type="JDBC"/>
			<dataSource type="POOLED">
				<property name="driver" value="${driver}"/>
				<property name="url" value="${url}"/>
				<property name="username" value="${username}"/>
				<property name="password" value="${password}"/>
				<property name="poolTimeToWait" value="${poolTimeToWait}"/>
			</dataSource>
		</environment>
	</environments>
	
	<!-- Mapper 설정 -->
	<mappers>
		<mapper resource="mybatis/member.xml"/>
		<mapper resource="mybatis/qnaBoard.xml"/>
		<mapper resource="mybatis/reviewBoard.xml"/>
		<mapper resource="mybatis/cart.xml"/>
		<mapper resource="mybatis/order.xml"/>
	</mappers>
</configuration>